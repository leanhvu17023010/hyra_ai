server:
  port: 8080
  servlet:
    context-path: /hyra_ai

spring:
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/hyra_ai?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

brevo:
  api:
    key: ${BREVO_API_KEY:}
  sender:
    email: ${BREVO_EMAIL:}

facefusion:
  base-url: http://172.16.1.75:8000

xtts:
  base-url: http://172.16.1.75:8020

whisperx:
  api:
    url: http://172.16.0.200:8000

ffmpeg:
  api:
    url: http://172.16.0.207:8000

jwt:
  signerKey: ${JWT_SIGNERKEY:"/nvphzRqEgduNfnKy2ztU3XBeojbmK8jzDivxOxLIGnl+D5GfkjyXdLq15xz9af/\n"}
  valid-duration: 7200
  refreshable-duration: 360000

app:
  frontend:
    base-url: "http://localhost:5173"

logging:
  level:
    root: INFO
    com.hyra_ai.backend: INFO
    # Giảm stack trace Netty (Connection reset) in ra console khi poll FaceFusion.
    reactor.netty.http.client: WARN
    reactor.netty.channel: WARN
    io.netty.channel: WARN

github:
client-id: "Ov23li9gzj8XBdJI3dxw"
client-secret: "d1090f9c88573ca3e2a9fa6f1087e30c3837010f"

cloudflare:
  r2:
    access-key: ${CLOUDFLARE_R2_ACCESS_KEY_ID:}
    secret-key: ${CLOUDFLARE_R2_SECRET_ACCESS_KEY:}
    endpoint: ${CLOUDFLARE_R2_ENDPOINT:}
    bucket: ${CLOUDFLARE_R2_BUCKET:}
    public-url: ${CLOUDFLARE_R2_URL:}